'use strict';

describe('riskApp.riskDetailsCapture module', function() {

  beforeEach(module('riskApp.riskDetailsCapture'));

  describe('riskDetailsCapture controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var riskDetailsCapture1Ctrl = $controller('riskDetailsCaptureCtrl');
      expect(riskDetailsCaptureCtrl).toBeDefined();
    }));

  });
});